{
  "name": "apphub-hub-host-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Vue 3 host for @kennofizet/apphub-frontend — deploy on a dedicated subdomain and embed in your product iframe.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@kennofizet/apphub-frontend": "^0.2.16",
    "axios": "^1.1.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
